# 환경 설정

이 프로젝트는 서버 환경을 `로컬`, `운영` 두 가지로만 관리합니다.

## 로컬 환경

### Frontend

1. `my-news-front/.env.local.example`을 참고해 `my-news-front/.env.local`을 작성합니다.
2. 기본값:

```env
NEXT_PUBLIC_API_BASE_URL=/api
BACKEND_BASE_URL=http://localhost:3000
```

3. 실행:

```bash
npm run dev:front
```

### Backend

1. `my-news-back/.env.local.example`을 참고해 `my-news-back/.env`를 작성합니다.
2. 필수값을 채웁니다.

```env
NODE_ENV=local
PORT=3000
CORS_ORIGIN=http://localhost:3001
ENABLE_SWAGGER=true
ENABLE_NEWS_SCHEDULE=true
ENABLE_STARTUP_NEWS_FETCH=true
DATABASE_URL=postgresql://...
NAVER_CLIENT_ID=...
NAVER_CLIENT_SECRET=...
```

3. 실행:

```bash
npm run dev:back
```

## 운영 환경

### Frontend

1. `my-news-front/.env.production.example`을 기준으로 `my-news-front/.env.production`을 작성합니다.
2. `BACKEND_BASE_URL`은 실제 API 도메인으로 설정합니다.

### Backend

1. `my-news-back/.env.production.example`을 기준으로 `my-news-back/.env.production`을 작성합니다.
2. 권장값:

```env
NODE_ENV=production
ENABLE_SWAGGER=false
ENABLE_STARTUP_NEWS_FETCH=false
ENABLE_NEWS_SCHEDULE=true
CORS_ORIGIN=https://your-front-domain.com
```

## 실행 명령

```bash
npm run build
npm run start:prod
```

## RAG 벡터 파이프라인 환경

뉴스 의미 검색과 이후 로컬 LLM 요약 기능의 기반으로 RAG 임베딩 파이프라인을 사용할 수 있습니다. 자세한 흐름은 `docs/rag-vector-pipeline.md`를 참고합니다.

```env
ENABLE_RAG_INDEXING=false
RAG_EMBEDDING_PROVIDER=ollama
RAG_EMBEDDING_MODEL=nomic-embed-text
RAG_CHUNK_SIZE=1200
RAG_CHUNK_OVERLAP=160
OLLAMA_BASE_URL=http://localhost:11434
```

로컬에서 사용할 때는 Ollama를 실행하고 `ollama pull nomic-embed-text`로 임베딩 모델을 준비한 뒤 `ENABLE_RAG_INDEXING=true`로 변경합니다.
